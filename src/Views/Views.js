export const Main = require('./MainView').default
export const Calc = require('./CalcView').default
export const WasteAndProfit = require('./WasteAndProfitView').default
export const Timer = require('./Timer').default

let Views = {
  Main: Main,
  Calc: Calc,
  WasteAndProfit: WasteAndProfit,
  Timer: Timer
}

export default Views